#ifndef CREF_HPP
#define CREF_HPP

#include <bits/stdc++.h>

void printString(std::string const &s) {
    std::cout << s << std::endl;
}

template <typename T>
void printT(T arg) {
    printString(arg);
}

#endif  // CREF_HPP
